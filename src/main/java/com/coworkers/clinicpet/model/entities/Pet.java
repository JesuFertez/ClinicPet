package com.coworkers.clinicpet.model.entities;import jakarta.persistence.*;import lombok.*;@Data@AllArgsConstructor@NoArgsConstructor@Entity@Table(name = "pet")public class Pet {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    @Column(name = "id")    @Getter @Setter private Long id;    private String name;    private String typeOfAnimal;    @Getter @Setter private int age;    @ManyToOne// relacion con owner    @JoinColumn(name = "owner_id", referencedColumnName = "id") //columna que guarda clave FK    private Owner owner;    @OneToOne    @JoinColumn(name = "medicalRecord_id", referencedColumnName = "id")    private MedicalRecord medicalRecord;    @OneToOne    @JoinColumn(name = "medicalHistory_id", referencedColumnName = "id")    private MedicalHistory medicalHistory;}