package com.coworkers.clinicpet.model.entities;import jakarta.persistence.*;import lombok.AllArgsConstructor;import lombok.Data;import lombok.Getter;import lombok.NoArgsConstructor;import lombok.Setter;@Data@AllArgsConstructor@NoArgsConstructor@Entity@Table(name = "pet")public class Pet {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    @Column(name = "id")    @Getter    @Setter    private Long id;    private String name;    private String typeOfAnimal;    @Getter    @Setter    private int age;    @ManyToOne// relacion con owner    @JoinColumn(name = "owner_id", referencedColumnName = "id", foreignKey = @ForeignKey(name = "FK_pet_owner"))    //columna que guarda clave FK    private Owner owner;    @OneToOne    @JoinColumn(name = "medical_record_id", referencedColumnName = "id",foreignKey = @ForeignKey(name = "FK_pet_medical_record"))    private MedicalRecord medicalRecord;    @OneToOne    @JoinColumn(name = "medical_history_id", referencedColumnName = "id", foreignKey = @ForeignKey(name = "FK_pet_medical_history"))    private MedicalHistory medicalHistory;}